# Páginas exentas del candado «color por variable de tema» (hex cromático grandfathered).
# VACÍO: toda la app usa variables de tema. Las paletas de data-viz migraron a tokens --dv-* (H-105).
# Referencia: src/pages/general/Perfiles.tsx (semántico) · Feriados/TableroCartera (paleta --dv-*).
# Si en el futuro una página necesita una excepción legítima, sumá su ruta acá (o comentario 'diseño-ok').
